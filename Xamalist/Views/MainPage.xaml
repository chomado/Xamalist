<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms" 
		prism:ViewModelLocator.AutowireViewModel="True" 
		x:Class="Xamalist.Views.MainPage" 
		Title="MainPage"
	>

	<!-- iOS のとき、上に 20px 余白 -->
	<ContentPage.Padding><OnPlatform x:TypeArguments="Thickness" iOS="0,20,0,0"></OnPlatform></ContentPage.Padding>

	<StackLayout>
		<!-- データ読み込みボタン -->
		<Button Text="データ読み込み" Command="{Binding ReadAppDataCommand}" />
		<!-- データ一覧リスト -->
		<ListView ItemsSource="{Binding AppDatas}">
			<ListView.ItemTemplate>
				<DataTemplate>
					<!-- メモ：ViewCell の中は複雑なレイアウトを組むことができる -->
					<ViewCell>
						<Label Text="{Binding Name}" />
					</ViewCell>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</StackLayout>
	
</ContentPage>
